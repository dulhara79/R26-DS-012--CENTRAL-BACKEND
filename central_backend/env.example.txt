# ── identity ─────────────────────────────────────────────────────────────────
# CRITICAL: a long random secret. Without it the backend refuses to hash MRNs.
# Generate with:  python -c "import secrets; print(secrets.token_urlsafe(48))"
MRN_PEPPER=CHANGE_ME_LONG_RANDOM_SECRET
PAIRING_CODE_TTL_MINUTES=30

# ── database ─────────────────────────────────────────────────────────────────
# dev:
DATABASE_URL=sqlite:///./central_backend.db
# NHSL deployment:
# DATABASE_URL=postgresql+psycopg://user:pass@localhost:5432/r26ds012

# ── component services (external; Central Backend is only an HTTP client) ────
C1_URL=https://dewdu-physiological-anxiety-escalation.hf.space
C2_URL=https://anxiety-component2-backend.vercel.app
C3_URL=https://dulharakaushalya-tc-wpn-demo.hf.space
C4_URL=https://YOURNAME-dcar-demographic-risk.hf.space
C1_TOKEN=
C2_TOKEN=
C3_TOKEN=
C4_TOKEN=
COMPONENT_TIMEOUT_S=60

# Test subjects for live_check.py (must already exist in each service)
C2_TEST_SUBJECT=P_65DC4002E7863773
C1_TEST_SUBJECT=test-user-1

# ── Uvindu Fusion Service (external HTTP service; no in-process fallback) ────
FUSION_URL=http://127.0.0.1:7861
FUSION_API_TOKEN=
FUSION_TIMEOUT_S=20

# ── CARE-AnxRAG (external HTTP service, NOT imported — see rag_client.py) ────
RAG_URL=http://127.0.0.1:8000
RAG_TOKEN=
RAG_TIMEOUT_S=60

# ── api ──────────────────────────────────────────────────────────────────────
BACKEND_API_TOKEN=r26ds012-backend-changeme

# ── end-user JWT verification (Phase 1) ─────────────────────────────────────
# These values MUST match the approved clinician/patient auth service.
# They are intentionally blank here because the project sources do not define
# production issuer/audience/algorithm/key values.
AUTH_JWT_ISSUER=
AUTH_JWT_AUDIENCE=
# Comma-separated allow-list, e.g. RS256 only if that is the auth service's
# verified contract. Never include "none".
AUTH_JWT_ALGORITHMS=
# Configure EXACTLY ONE key source:
AUTH_JWT_JWKS_URL=
AUTH_JWT_PUBLIC_KEY=
AUTH_JWT_SECRET=

# BACKEND_API_TOKEN is legacy/service configuration only. Phase 1 clinician and
# patient authorization does NOT accept it as an end-user identity.
