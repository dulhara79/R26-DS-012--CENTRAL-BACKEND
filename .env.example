# Use central_backend/env.example.txt as the canonical baseline configuration.
# Copy it to central_backend/.env for local execution.
#
# This root file intentionally does not redefine service contracts or secrets.
